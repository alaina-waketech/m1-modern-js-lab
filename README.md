# m1-modern-js-lab
m1 js lab

M1 questions:
1. Frontend resposibilities tend to be anything a user interacts with. Frontend handles page displays, user interactions on the page, and sending requests to the backend. It mainly involves html, css, and javascript. Backend deals with the behind the scenes processes, like proccessing request, data validation, and returning info to the front end.
2. User completes and action and then the frontend sends a request. Then the backend recieves the request and looks in the database. The server will send a response and then the front end will display that to the user.
3. Using modular file organization helps break large code into smaller, easier to edit chunks. 
